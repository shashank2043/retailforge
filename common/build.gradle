apply plugin: 'java-library'

dependencies {
    // Inherits Spring Boot and validation from parent
    api 'org.springframework.boot:spring-boot-starter-web'
    compileOnly 'jakarta.persistence:jakarta.persistence-api:3.1.0'
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
